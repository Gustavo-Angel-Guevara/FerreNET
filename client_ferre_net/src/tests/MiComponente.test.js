import React from 'react';
import {render} from '@testing-library/react';
import Input from '../components/Input';

test('renders MiComponente correctly', () => {
    render(<Input />);
    // Aquí puedes realizar las afirmaciones (assertions) sobre el componente renderizado
});